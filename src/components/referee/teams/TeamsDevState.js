const number = Math.floor((Math.random() * 10000) + 1);
const team = 'Team' + number;


const TeamsDevState = {
  team: {
    name: team,
    city: 'City',
    location: 'Locaiton'
  }
};

export default TeamsDevState;
